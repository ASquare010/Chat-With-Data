[project]
name = "db-faker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "common",
    "pandas>=2.3.3",
]

[tool.uv.sources]
common = { workspace = true }
